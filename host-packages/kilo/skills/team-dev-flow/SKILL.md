---
name: team-dev-flow
description: "Use Team Dev Flow in Kilo Code for provider-neutral specification, task intake, bounded implementation, exact-SHA review, convergence, and GitHub handoff."
---

# Team Dev Flow for Kilo Code

Read the canonical router at `../../../.team-dev-flow/plugin/skills/index/SKILL.md` completely, then follow its referenced skill inside `../../../.team-dev-flow/plugin/`. Resolve all relative links from the canonical file, not from this wrapper.

Use the active FlowProfile. `kilo-github` reads GitHub Issues and renders exact non-authorizing Issue/PR previews; `obsidian-local` operates only in the selected TaskStore root. No preview, model message, digest, Kilo auto-approval, or installed credential grants a remote write, push, PR, merge, release, or deploy.

Kilo reloads skills at a new session or `/reload`. If the installed manifest fails `host_package.py check`, stop and reinstall from a reviewed Team Dev Flow package before work continues.
